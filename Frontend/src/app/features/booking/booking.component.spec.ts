/// <reference types="jasmine" />

import { BookingComponent } from './booking.component';

describe('BookingComponent', () => {
  it('should create the component', () => {
    const component = new BookingComponent();
    expect(component).toBeTruthy();
  });
});
